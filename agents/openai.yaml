interface:
  display_name: "PPT HTML Generator"
  short_description: "Generate validated HTML slide decks for editable PPTX export."
  default_prompt: "Use $ppt-html-gen to generate a browser-previewable HTML deck and validate it for PPTX export."

policy:
  allow_implicit_invocation: true
