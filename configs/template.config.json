{
  "activeTemplate": "bit-template",
  "registryFile": "configs/template.registry.json",
  "notes": "Template definitions live in templates/<template-id>/template.config.json. This file keeps backward-compatible active-template metadata."
}
